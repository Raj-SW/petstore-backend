name: SonarCloud

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  sonarcloud:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      # Generate coverage so Sonar reports the real % instead of 0.0%.
      # A single Jest run over both projects auto-merges coverage into
      # coverage/lcov.info (the path sonar-project.properties points at).
      - name: Generate coverage
        run: npm run test:coverage

      - name: SonarCloud Scan
        uses: SonarSource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_CUBE_TOKEN }}
