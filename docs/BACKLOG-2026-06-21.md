# VitalPaws Backlog — captured 2026-06-21

Shared backlog for the team. Items span **both repos** — `petstore-backend` (BE) and `petstore-frontend` (FE). Grouped into epics; each item tagged with repo, type (🐛 bug / ✨ feature / ♻️ refactor), and rough size (S/M/L). Brainstorm → spec → plan happens per epic before implementation. Nothing here is started.

> Related existing docs: `docs/superpowers/specs/2026-06-21-product-bulk-actions-and-image-ordering-design.md` (product image reorder/set-primary is already speced — see Epic 6), `docs/SECURITY-REMEDIATION-2026-06-21.md` (separate security queue).

---

## Epic 1 — Navigation & layout (FE)
- **About Us in navbar** — page exists at `/about` but has no nav entry. 🐛 S
- **Mobile header breaks** — too many navbar items overflow on small screens. Proposed: hide the "VitalPaws" wordmark on mobile, move the currency switcher into the side/drawer panel. 🐛/✨ M
- **Breadcrumbs missing on Pet Gallery** — breadcrumb strip absent on the gallery pages. 🐛 S

## Epic 2 — Design-system UI overhaul (FE) — UMBRELLA / program
- **Full UI rebuild** — replace the current "full UI" with Radix UI + React Bits + 21st.dev components + the frontend design skills. This is an umbrella program; several items below are concrete instances and should likely be done under it. ✨ L (decompose further)
- **Style all dropdowns** — replace native `<select>` UI everywhere with components using our design tokens. ✨ M
- **Rich-text-editor images must not break layout** — images inserted in the RTE overflow/break the UI; constrain/responsive-ize them. 🐛 S/M
- **Reuse the custom search component** across Service page, Appointments page, and Pet Care Tips (replaces the gimmicky particle SearchBar; prior sessions deferred generalizing it). ✨ M

## Epic 3 — Service page (FE)
- **Service cards → correct links** — each card routes to its real page. 🐛 S
- **"Coming Soon" state** — unimplemented services show a Coming Soon badge instead of a dead link. ✨ S
- **Use the custom search component** (see Epic 2). ✨ S

## Epic 4 — Professionals & Appointments (FE)
- **New professional card** — beautiful, aesthetic, responsive redesign. ✨ M
- **Appointments page uses the custom search bar** (see Epic 2). ✨ S

## Epic 5 — Pet Shop (FE, maybe BE)
- **Side-panel filtering broken** — the left filter panel does not actually filter results; fix. 🐛 M
- **Remove the filter strip** after the hero section. 🐛 S

## Epic 6 — Product images, variants & pricing (BE + FE)
- **Sort which product image shows first** — admin reorders images / sets primary. ✅ ALREADY SPECED (`2026-06-21-product-bulk-actions-and-image-ordering-design.md`, Feature 2). ✨ —
- **Variant-specific images** — admin uploads images per weight/size variant; product detail swaps images when a variant is selected. Needs a schema change (images on the `variants[]` subdoc) + upload flow + frontend selector wiring. ✨ M/L
- **Products default to MUR** — when an admin creates a product, prices are **MUR (Rs)** by default. Surface the currency on the price/variant-price inputs, and make MUR the explicit/stored default so product pricing is unambiguous. Part of app-wide currency standardization (the invoice PDF currently prints `$` — see Epic 11 and the cross-cutting currency note). ✨ S

## Epic 7 — Feedback / testimonials (BE + FE)
- **🐛 Photo↔feedback correlation bug** — in homepage "What Our Clients Say", photos shown don't belong to their feedback. `Feedback.photos` is `[String]` per-doc; likely a bug in the create/upload flow (parallel-upload ordering) or the render mapping. Needs debugging. 🐛 M
- **Admin reorders feedback images** — choose display order of a feedback's photos (same reorder pattern as product images). ✨ S

## Epic 8 — Pet Care Tips (BE + FE)
- **Richer authoring** — admin adds: section structure (exists as text `sections[]`), **cover image**, **per-section images**, and **images inside the rich-text body**. Cover/section images are new (schema + upload). ✨ M
- **Use the custom search component** (see Epic 2). ✨ S
- (RTE image overflow — see Epic 2.) 

## Epic 9 — Announcements (BE + FE) — generalize beyond sales
- **🐛 Email redirect link wrong URL** — the CTA/redirect link in announcement emails goes to the wrong URL. Fix. 🐛 S
- **Generalize `SaleAnnouncement` → Announcements** — currently hard-wired to `products[]` + `source`. Add an announcement **type/category** so admins can announce: sales, **events, new products, new tips/blog posts**, and more (brainstorm candidates: back-in-stock/restock, gallery posts, service launches, price drops, holiday hours/closures, loyalty/promo). Each type needs an appropriate target (products vs tip vs event details vs free-form) + email template. ✨ L

## Epic 10 — Email templates (BE)
- **Audit & unify** all transactional + marketing templates (welcome, login, reset, order-*, payment-*, contact-reply, announcement, subscription) for consistent branding and aesthetics. Note: `resendVerification` currently uses the password-reset template (also in security queue F5). ♻️ M

## Epic 11 — Invoices (BE) — granular detail
- **Full granular invoice** — brainstorm and expand to include: per-line items (with variant label, unit price, sale/effective price, per-line discount, qty, line total), subtotal, order-level discount + code, shipping, tax, grand total, currency, billing + shipping address, payment method, transaction id, invoice number, issue/paid dates, customer details. ✨ M

## Epic 12 — Subscriptions (BE + FE) — largest BE epic
- **Admin: view subscriptions** — list/detail of each user's subscriptions and the subscribed items (product + variant + qty + interval + next run + status). ✨ M
- **Flag subscribed products** — in the admin product list, flag products that appear in any active subscription. ✨ S/M
- **Subscription analytics + inventory prediction** — forecast upcoming demand from active subscriptions (sum of qty × runs in window per product/variant) vs current stock; surface restock-needed predictions. ✨ L
- **User: view own subscriptions** — granular detail page for the customer's own subscriptions. ✨ M

## Epic 13 — Import / Export (FE) — big rebuild
- **Merge & redo** — merge the import/export page into the import-export-service page form; "delete and redo everything." Prior session flagged `ImportPage.jsx` as a 920-line 3-step wizard deliberately kept separate — user now wants it consolidated and rebuilt. ♻️/✨ L

---

## Suggested sequencing (proposal, not locked)
1. **Quick-win bug batch** (1 day-ish): About-Us nav, gallery breadcrumbs, announcement email URL, petshop filter-strip removal, service-card links + Coming Soon. Mostly FE, low risk.
2. **Petshop side-panel filter fix** + **feedback photo-correlation bug** (need debugging).
3. **Design-system foundation** (dropdowns, search-component reuse, RTE image fix) — unblocks Service/Appointments/Tips items and the broader UI rebuild.
4. **Feature epics**, brainstormed individually: Announcements generalization, Subscriptions (admin/user/analytics), Invoices granular, Variant images, Tips authoring, Professional card.
5. **Large rebuilds**: Import/Export redo, full UI overhaul (as an ongoing program).
